export APP_ROOT=$(pwd)
export APP_PORT=43
set-window-title $(basename $APP_ROOT):${APP_PORT}

export DOCKER_WRAPPER_RC=$APP_ROOT/.docker-wrapper.rc

export LOG_LEVEL=DEBUG

export DOMAIN="$USER.dev.getto.systems"
export COOKIE_DOMAIN=$DOMAIN

export AWS_CLOUDFRONT_KEY_PAIR_ID=$(cat $APP_ROOT/.tmp/aws/key_pair_id.txt)
export AWS_CLOUDFRONT_PEM=$APP_ROOT/.tmp/aws/pk.pem
export AWS_CLOUDFRONT_SECURE_URL="https://secure.example.getto.systems/*"

export TICKET_PRIVATE_KEY=$APP_ROOT/.tmp/jwt/ticket/pk.pem
export TICKET_PUBLIC_KEY=$APP_ROOT/.tmp/jwt/ticket/pub.pem

export API_PRIVATE_KEY=$APP_ROOT/.tmp/jwt/api/pk.pem

export ADMIN_USER_ID=RANDOM-GENERATED-ID
export ADMIN_LOGIN_ID=admin
export ADMIN_PASSWORD=$(cat $APP_ROOT/.tmp/admin_password.txt)
